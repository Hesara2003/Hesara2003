name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}

          # --- BASE CONFIG ---
          user: hesara2003
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Colombo

          # --- 1. ISOMETRIC CALENDAR (3D Blocks) ---
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year

          # --- 2. LANGUAGES (Bar Chart) ---
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%

          # --- 3. TOPICS (Icons at bottom) ---
          plugin_topics: yes
          plugin_topics_limit: 15
          plugin_topics_mode: icons

          # --- 4. LINES ("Weird or Wired") ---
          plugin_lines: yes
          plugin_lines_history_limit: 1
          plugin_lines_repositories_limit: 4
          plugin_lines_sections: base

          # --- 5. HABITS (Coding Habits) ---
          plugin_habits: yes
          plugin_habits_charts_type: classic
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_from: 200
          plugin_habits_languages_limit: 8
          plugin_habits_languages_threshold: 0%

          # --- 6. TRAFFIC (Views) ---
          plugin_traffic: yes

          # --- 7. PROJECTS (Roadmap) ---
          plugin_projects: yes
          plugin_projects_limit: 4

          # --- 8. REPLACEMENT: ACHIEVEMENTS (Badges) ---
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: C

          # --- 9. REPLACEMENT: NOTABLE CONTRIBUTIONS ---
          # Shows popular repos you helped (instead of Music)
          plugin_notable: yes
          plugin_notable_from: organization
          plugin_notable_types: commit
